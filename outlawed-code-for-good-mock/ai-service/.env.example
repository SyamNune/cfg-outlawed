# Service configuration
PORT=5001
CLIENT_URL=http://localhost:5173

# Set to "mock" until MongoDB Vector Search and an embedding provider are connected.
RETRIEVAL_MODE=mock

# Local BGE-M3 embedding service (started separately on port 8001).
EMBEDDINGS_SERVICE_URL=http://127.0.0.1:8001
